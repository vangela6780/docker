# WSL2 Ubuntu Proof

Command run on Windows host:

`wsl -l -v`

Output:

```
  NAME              STATE           VERSION
* Ubuntu            Running         2
  docker-desktop    Running         2
```

This confirms Ubuntu is installed and running on WSL2.
